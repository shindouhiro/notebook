1. [[请详细描述一下 **React Fiber** 是如何解决传统堆栈调和（Stack Reconciler）的痛点，并实现异步可中断更新的]]

2. 在 React 18 之后，**Concurrent Mode（并发模式）** 的引入改变了渲染优先级。请谈谈 `useTransition` 和 `useDeferredValue` 的底层区别及适用场景。”

3. 如果一个父组件频繁重绘（re-render），你会如何排查并优化？除了 `memo`、`useCallback`，你会如何设计组件结构来‘跳过’不必要的更新？

 4. 聊聊 **React Server Components (RSC)**。它和传统的 **SSR (Server Side Rendering)** 有什么本质区别？在架构层面，RSC 解决了什么样的数据传输痛点？”

5. 从状态管理架构上，你会选择什么样的方案（Context, Redux, Zustand, 还是原生 Hooks）？理由是什么？

 6. 考虑到网络波动和高频输入，你会如何在 React 层面优化编辑器的响应性？”


